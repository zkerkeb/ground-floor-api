'use strict';

/**
 * oversensitive-guideline service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::oversensitive-guideline.oversensitive-guideline');
