'use strict';

/**
 * oversensitive-guideline router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::oversensitive-guideline.oversensitive-guideline');
