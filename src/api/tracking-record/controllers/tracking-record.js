'use strict';

/**
 * tracking-record controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::tracking-record.tracking-record');
