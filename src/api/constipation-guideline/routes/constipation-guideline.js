'use strict';

/**
 * constipation-guideline router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::constipation-guideline.constipation-guideline');
